The generated references are attached after the five parent-video frames and are labeled ${picture_tags}. A picture listed as an edited primary anchor or temporal anchor is a concrete frame/composition reference: include its exact <Picture N> label and copy its mapping exactly in subject_definitions, then refer to it consistently in the other sections. Use a standalone <Picture N> only for such a concrete frame or composition anchor; if an attached image is only an appearance guide, describe its visible appearance as a <Subject N> and mention the supporting picture. Keep <Video 1> as the source of motion and action. The mandatory anchor mappings are:
${role_contract}
In retention_analysis mark each attached reference as fully_preserved, partially_preserved, or weak_reference with one short visible reason. Do not invent references, change their roles, or add audio sections.
