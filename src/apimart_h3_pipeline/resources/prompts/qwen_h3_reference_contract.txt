The generated references are attached after the five parent-video frames and are labeled ${picture_tags}. Use a standalone <Picture N> only when an attached image is a concrete frame or composition anchor; otherwise describe its visible appearance as a <Subject N>. Keep <Video 1> as the source of motion and action. If a picture is a frame anchor, copy this mapping exactly:
${role_contract}
In retention_analysis mark each attached reference as fully_preserved, partially_preserved, or weak_reference with one short visible reason. Do not invent references or add audio sections.
