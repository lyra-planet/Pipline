The generated references are attached after the five parent-video frames and are labeled ${picture_tags}. In subject_definitions, keep <Video 1> as the source video and subject/action/motion source. Use a standalone <Picture N> only when the attached image is a concrete target frame or composition/keyframe anchor; if it is appearance-only, define a <Subject N> and mention the corresponding image there instead. Copy the authoritative picture-to-frame mapping below exactly when a picture is a frame anchor:
${role_contract}
In retention_analysis mark each reference as fully_preserved, partially_preserved, or weak_reference and explain only the visible relationship. In detailed_description keep the video as the primary subject and describe any frame-anchor appearance lock at the corresponding source frame. Do not add audio sections or any reference that is not attached.
