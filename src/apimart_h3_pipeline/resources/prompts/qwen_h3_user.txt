Raw atomic edit requirement:
${raw_prompt}

Read the five source-video frames in time order and any attached pictures. Return only these four sections, with no text before or after them.

subject_definitions:
Define <Video 1> as the source video. Define each important visible subject separately, for example the person as <Subject 1> and the visible environment as <Subject 2>. Mention only details that are clearly visible. Do not invent a picture when none is attached.

summary:
Start with [video editing]. The first sentence for video editing must be "The target video is an edited version of <Video 1>." State the requested operation and the visible elements that stay unchanged. If the operation changes the camera, say that frame 0 remains exactly unchanged and that the requested camera change begins immediately after frame 0. Do not describe an environment as having an identity or action.

retention_analysis:
For <Video 1>, use partially_preserved when the requested operation changes any part of the video, and explain which part changes. For an unchanged person, object, or environment, use fully_preserved and describe preserved appearance or content, never an environment's action. Use weak_reference only for a general visual reference. Give one short reason for each entry.

detailed_description:
Describe the source shot and the requested operation in playback order. Name the actual camera or visual operation from the raw requirement and describe how it affects only the requested target. For a camera operation, state that frame 0 is exactly unchanged and that the camera change begins immediately after frame 0; preserve the subject's identity, action, focus, scale, and framing as closely as possible without forcing a centered position. For a non-camera operation, preserve camera motion and all unrelated visual content unless the raw requirement changes them. Do not add unrequested people, objects, actions, lighting, scene changes, effects, camera operations, cuts, or audio.
