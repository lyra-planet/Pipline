Raw atomic edit requirement:
${raw_prompt}

Inspect all five parent-video frames in temporal order and every attached reference. Return the final prompt text directly as exactly these four sections: subject_definitions, summary, retention_analysis, detailed_description. In subject_definitions define <Video 1> as the source video, then define every important visible subject or visual characteristic that must be preserved or followed. Use a <Picture N> entry only when its attached image is a concrete target frame, keyframe, or composition anchor; when an image supplies only appearance, define a <Subject N> and mention that image inside the subject definition instead of making a standalone picture entry. In summary begin with [video editing] (or the appropriate official reference-generation/keyframe prefix), then begin the video-edit text with "The target video is an edited version of <Video 1>." State only the requested visual change and what remains unchanged. In retention_analysis give each important reference exactly one relationship marker: fully_preserved, partially_preserved, or weak_reference. In detailed_description describe playback order and the original scene, composition, subject appearance/position/action, requested camera or visual edit, focus behavior, and unchanged elements. Use precise camera language when relevant (for example smooth horizontal pan to the left, no zoom, no tilt, no rotation, no cuts). Do not add people, objects, actions, lighting, scene changes, effects, camera movements, or audio details that are not requested or visible. Keep all four sections in English and do not add any fifth section.
